---

- name: install node packages
  become_user: "{{ user }}"
  npm:
    name: "{{ item }}"
    global: yes
  with_items:
    - pyright
    - neovim
  tags:
    - install_packages
    - install_packages.node_packages

