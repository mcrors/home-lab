---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-plex
  namespace: plex
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 12Gi  # adjust to match your old volume

---
apiVersion: batch/v1
kind: Job
metadata:
  name: migration-job
  namespace: plex
spec:
  template:
    spec:
      nodeName: lib-pi-05
      containers:
        - name: migrate
          image: alpine:latest
          command: ["/bin/sh", "-c"]
          args:
            - |
              apk add --no-cache rsync
              rsync -av --progress /old-data/ /new-data/
              echo "Migration complete"
          volumeMounts:
            - name: old-data
              mountPath: /old-data
            - name: new-data
              mountPath: /new-data
      restartPolicy: Never
      volumes:
        - name: old-data
          persistentVolumeClaim:
            claimName: pvc-migrate-iscsi-plex
        - name: new-data
          persistentVolumeClaim:
            claimName: pvc-plex
  backoffLimit: 0
