---

- hosts: all
  become: true
  remote_user: hla
  roles:
    - common
    - set_static_ip
  vars_files:
      - vars/main.yaml
  tags: common

- hosts: docker
  become: true
  remote_user: hla
  roles:
    - role: docker
  tags: docker

