---

- name: Install cryptsetup
  apt:
    name: cryptsetup
    state: present
    update_cache: yes

- name: Load kernel modules for Longhorn
  community.general.modprobe:
    name: "{{ item }}"
    state: present
    persistent: present
  loop:
    - nfs
    - dm_crypt
