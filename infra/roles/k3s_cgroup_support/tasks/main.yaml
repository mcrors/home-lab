---

- name: cgroup support enabled by default on Armbian Trixie (previously required cmdline.txt modification)
  debug:
    msg: "cgroups v2 unified hierarchy active - no kernel param changes needed"
  when: ansible_facts['os_family'] == "Debian"
